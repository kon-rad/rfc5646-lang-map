const assert = require('assert');
const RFC5646 = require('../build/index.js');

describe('getName', () => {
  it('en', () => {
    console.log('RFC5646:',RFC5646);
    // assert(RFC5646.getName('en'), 'English');
  })
});